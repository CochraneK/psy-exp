# 2026-08-31

## MCCB 全面审查及改进 — 修复阶段（已闭环）

完成「全面审查及改进」审查基线 + 全部 8 处修复。

### P0
- mccb-msceit.html：缺失 `<script>` 开标签（仅 `'use strict';`，JS 全部未执行、测验瘫痪）→ 在 `</main>` 后补 `<script>`；另修复结果页游离 `</footer>`（缺 `<footer class="result-actions" id="resultActions">` 开标签）。

### 选择器统一（DEV/USER 模式）
- 10 个测试页 applyModeStyles 选择器统一为 `.count-badge, .feedback-area, .dev-hint, .progress-text, .quick-fb, #counterText, #devHintBacs`
- 移除 `.key-legend, #keyLegend`：BACS 符号-数字对照表是答题必需材料，USER 模式曾误隐藏；其他页无该元素（no-op）
- Fluency 补回 `.count-badge`：USER 模式隐藏「已记录 N 个动物」计数器，与全局一致

### 其他逻辑修复
- mccb-tmt.html：练习完成提示「100 秒」→「300 秒」（与 STATE.timeLimit.A=300 一致）
- mccb-hvlt.html：startTrial 在 trialNum===1 时重置 STATE.results，避免中途返回首页重来时混入上一轮成绩
- mccb-bvmt.html：submitRecall 对错着色（correct/incorrect/wrong-pos）加 `if(IS_DEV)` 守卫，评分逻辑不变；USER 模式不再显示反馈色
- mccb-cpt.html：showExport 移除自动 `a.click()` 下载，改为显式「下载 JSON / 复制」按钮（与其他页弹窗+手动导出一致）

### 验证
- Node vm.Script 编译检查：10 个文件全部 inline script 语法 OK（MSCEIT 由 0 → 1 有效脚本，确认 P0 修复）

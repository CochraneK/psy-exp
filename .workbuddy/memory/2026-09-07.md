# 2026-09-07 工作日志

## 完整10项测验自动化模拟 — 全部通过

**任务**: 模拟被试"test"完整完成全部10项MCCB认知测验

### 关键修复 — BVMT驱动
BVMT从第2次recall到delayed recall切换时，页面停留在同一个 `#page-recall`，驱动程序用 `__placed` 标志防止重复执行，但延迟回忆页显示时 `__placed` 未重置（因为else分支只在非recall页面触发）。

**修复**: 在驱程中增加 `__lastTrial` 追踪 STATE.trial 变化，比用 `__placed` 更可靠：
```javascript
if(!window.__placed||window.__lastTrial!==cur){window.__placed=true;window.__lastTrial=cur;
  for(var i=0;i<6;i++){selectPoolShape(TARGETS[i]);placeShape(i)}
  submitRecall();
}
```

### agent-browser 升级适配
- agent-browser 从旧版(有`start --port`)升级到 v0.27.0(无start命令，隐式daemon)
- eval 探活 `1+1` → `1 + 1`（v0.27 + 号需空格包围）
- 静态文件需要独立的HTTP服务器(用Python `http.server 8766`)

### 测试结果
- 全部10项: ✅ completed
- 总用时: 331秒 (约5.5分钟)
- 修复涉及文件: `tests/full-battery.cjs` (BVMT driver + eval探活)

## 改进/纠错/完善 (第二波, 2026-09-07 上午)

`tests/full-battery.cjs` 系统性升级:
1. **REPORT 目录自动创建**: `fs.mkdirSync(OUT_DIR,{recursive:true})`, 目录缺失不再崩溃
2. **环境变量化**: `MCCB_BASE`(默认 http://127.0.0.1:8766) + `MCCB_PARTICIPANT`(默认 test)
3. **HTTP server 前置探测**: net.connect 检查 8766 是否可达, 不可达给出启动提示 (agent-browser v0.27 不自带静态服务, 需 `python -m http.server 8766`)
4. **数据完整性断言**: 全量跑后强制校验 completed 数 === order 数, 未达标设 `process.exitCode=2`
5. **HTML 报表生成**: `renderReportHtml()` 产出可读可视化报表, 全量写 .html
6. **--render 模式**: 无需重跑测试, 从最新 JSON 重新生成 HTML (兼容无 integrity 字段的旧 JSON)

最终产物: `data/autotest/battery-test-report.json` (33KB 全量) + `battery-test-report.html` (10/10 可视化)

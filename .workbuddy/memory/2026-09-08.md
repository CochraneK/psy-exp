# 2026-09-08 工作日志

## full-battery.cjs 再一次提升 (CLI 完善)

在 9/7 基础上继续增强（首用时机完成 10/10 后）：
1. **CLI 参数解析**：`--help/-h`、`--list`、`--retry N`、`--timestamp`、`--render`
2. **单测验失败自动重试**：`runOne` 包 `runOneAttempt`，`--retry N` 抗 daemon 抖动
3. **时间戳报表**：`--timestamp` 保留历史，不覆盖
4. **`order` 提为顶层常量**：`--list`/`--help`/`main` 共用
5. **数据完整性断言改**：全量跑 `completed >= selectedOrder.length` 才算通过，否则退出码 2
6. `--list`/`--help`/`--render` 均验证通过

## Neat Freak 知识库清理（@skill 调用）

项目缺 README/docs（有可运行代码阶段），本次补齐交接基础 + 毕业稳定知识：
- **新增 `README.md`**：项目概览、目录结构、10 项测验表、双模式、被试系统、自动化测试入口（面向新人）
- **新增 `docs/automation.md`**：自动化测试完整运行指南——full-battery 用法/产物/退出码、agent-browser v0.27 适配要点、驱动踩坑记录（localStorage 判真/DIV disabled/input 事件/BVMT 延迟回忆）、测试脚本目录速查（"系统怎么工作"的稳定知识，从 memory 毕业到 docs）
- **MEMORY.md**：加"自动化测试"节（指针指向 docs/automation.md，不重复机制）；141 行/6.1KB 仍健康
- **规范审计**：无 CLAUDE.md/AGENTS.md（项目无规则体系）；README/docs 引用路径全部核验存在（无死引用）；无相对时间残留

### 自检结论
- MEMORY.md ≤ 200行/25KB ✅ (141/6.1KB)
- 相对时间清零 ✅
- README/docs 无死引用 ✅
- 过期开放项已核实（9/4 两条"待做"：真机烟测、自动答题模拟均已落地）

## 一句话
把"系统怎么工作"的自动化测试知识从松散 memory 毕业进 docs/automation.md，补齐了 README，让下个会话/新接手者能直接跑起来。

## doc/ 与 docs/ 目录合并（20:45）

用户指出"目前有两个"文档目录，要求合并：
- 原 `doc/`（4 个资料：简易韦氏 PDF、操作者表格 A/B TXT、队列研究者手册 PDF）
- 新建 `docs/`（automation.md）
→ 合并为单一 `docs/`：移动 4 个资料文件入 docs/，删除空 doc/，更新 README 两处 `doc/` 引用为 `docs/`。
验证：无重名冲突、全项目无 `doc/` 残留引用、历史日志的 doc/ 为快照保留不动。

## 删除 automation.md，核心信息并入 README（21:08）

用户要求删除 docs/automation.md、把核心信息写入 README：
- 删除 `docs/automation.md`
- README「自动化测试」节扩充：前置条件（agent-browser + http.server 8766）、完整用法（全量子集/--retry/--timestamp/--render/--list）、环境变量、产物、退出码（0/2）、架构（页面内驱程 + 同标签页导航）、关键踩坑（localStorage "false" 恒真 / DIV disabled / input 事件 / v0.27 运算符空格 / 禁用 .cmd 包装）
- 同步：README 目录结构 docs/ 行去 automation.md；MEMORY.md 指针从 docs/automation.md 改为 README「自动化测试」节
- 验证：全项目无 automation.md 残留引用（Grep）

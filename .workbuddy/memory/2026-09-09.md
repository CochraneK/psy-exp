# 2026-09-09 工作日志

## 继续改进（继 9/8 结构优化后）

### 修复 validate-drivers.cjs
- 上次给 full-battery.cjs 增加 CLI 参数（`process.env`/`process.argv`）后，沙箱无 `process` 变量导致 DRV 提取失败
- 修复：添加 `process: { env: {}, argv: [], exit(){}, exitCode: 0 }` mock + 丰富 require stub 函数
- 验证：10/10 全部通过 ✅

### 修复 index.html 卡片初始状态闪烁
- 10 张测试卡硬编码 `<span class="tc-status done">✓ 已完成</span>`，JS 加载后才纠正 → 可见闪烁
- 修复：全部替换为 `<span class="tc-status">—</span>`，JS 动态设置正确状态
- 验证：10/10 替换 ✅，JS 中 `className = 'tc-status done'` 不受影响

### 增强 cpt_report.py：批量处理模式
- 新增 `--all` / `--batch` 参数：自动扫描 data/cpt-results/ 下所有 .trep 文件
- 241 个 .trep 文件全部处理完成，0 失败 ✅
- 产物：data/cpt-reports/ 下 239 个独立 HTML 报表（含内联 SVG 图表）

### README 更新
- 补充 validate-drivers 说明（语法沙箱校验）
- 新增「CPT 报告生成」节（单文件 + 批量用法）

## 被试数据管理功能（第二轮）

### 发现：importData 中 KEY_TO_RESULT 引用失效
- importData() 在 index.html 中引用 `KEY_TO_RESULT`（定义于 mccb-participant.js 内部作用域，未导出到 window），导入时若被试已登录会抛 ReferenceError
- 修复：在 index.html 中定义 `IMPORT_KEY_TO_RESULT` 常量替代

### mccb-participant.js 扩展
- 新增 `deleteParticipant(cohortId)` — 删除被试数据对象 + 向后兼容键（`mccb-participant-{ID}-mccb-*-result`） + 从列表移除 + 自动登出
- 新增 `getAllSummaries()` — 返回所有被试元数据数组（id/done/total/createdAt/updatedAt/isCurrent），用于列表展示
- 新增 `exportAllData()` — 打包全部被试数据为 JSON 对象

### index.html 新增功能
- **被试管理按钮**（👥 被试管理）位于仪表盘动作栏左侧
- **被试管理模态框**：列表展示所有被试（编号/进度条/百分比/最近更新时间/删除按钮）
- **删除被试**：弹窗确认后清理该被试全部数据，自动刷新界面
- **导出全部**：一键导出所有被试数据为 JSON（`mccb-all-participants-{date}.json`）
- **clearAllData 重写**：登录态自动删除全部被试数据而非仅 global key；未登录态仍清 global
- 点击外层背景关闭模态框
- README 同步更新被试管理说明

## 第三轮改进：CPT 索引 + 颜色统一 + localStorage 安全

### CPT 报告索引页
- 创建 `data/cpt-reports/index.html`：搜索/排序/筛选/分页的完整索引页
- 扫描 239 份报告生成 `reports-index.json`（被试 233 人，男 114 / 女 125）
- 统计摘要：总报告数、被试数、性别分布、多测人次
- 按被试编号搜索、性别/多次测试筛选、点击表头排序

### 硬编码颜色替换（60 处 → 0 处）
- 向 common.css 新增 3 个语义变量：`--color-success-bg`、`--color-danger-bg`、`--color-warning-bg`
- 批量替换 10 个测试页中 60 处硬编码颜色为 CSS var() 引用
- 保留 Canvas 绘图颜色（BVMT 形状填充、Mazes 渐变代码）和页面独特色调（logo 渐变、紫/橙主题色）
- 验证：SVG/JS 形状数据完整未损坏

### localStorage 安全封装
- 新增 `_ls` 工具对象，对所有 localStorage 操作包裹 try/catch
- 静默捕获 `QuotaExceededError`（存储满）、隐私模式禁用等异常
- `set()` 失败时有 console.warn 日志，其余静默
- 使用 `_ls.json()` 简化 `JSON.parse(localStorage.getItem())` 模式

## 第四轮：综合认知报告

### mccb-scoring.js 评分模块
- 新建 `mccb-scoring.js`，统一读取各测验结果
- 定义 MCCB 7 个认知域：处理速度、注意/警觉、工作记忆、言语学习、视觉学习、推理与问题解决、社会认知
- 各测验关键指标提取（10 项全覆盖）
- 内部相对排名：收集所有被试的域原始分 → 百分比排名 → T 分近似转换（正态假设）
- 修复 KEY_TO_RESULT 映射问题：`data.results` 实际以 `mccb-cpt-result` 等长键存储，需查表

### comprehensive-report.html 综合报告页
- 新建独立 HTML 页面，顶部被试选择器
- Canvas 雷达图绘制 7 域 T 分轮廓（自适应亮/暗色主题）
- 域评分卡片：T 分 + 百分比 + 进度条
- 测验详情表：每个测验的核心原始指标
- 导出功能：JSON 下载 / 打印 PDF / 复制文字摘要
- 首页「📊 综合报告」按钮链接到此页

## 第五轮：Neat Freak 知识库审计（2026-09-09）

- 运行 Neat Freak 技能，对 README.md + MEMORY.md 做洁癖级审查
- 修正事实性过期：common.css 行数 ~330→365、CSS 自定义属性 24→53（MEMORY.md 与 README.md 两处一致修正）
- 删除 MEMORY.md 空的「各页面核心特征速查（更新于 2026-08-29）」死 header（紧跟 CSS 节无内容）
- 新增 MEMORY.md「近期模块与陷阱」节：综合评分非官方常模/验证命令、`window.ParticipantManager` 第 278 行加载时挂载陷阱、`.rep/.dat/.raw` 二进制死路
- 规则层审计：README 引用路径全部存在、10 测验页齐全、无死引用、无体量倒挂（memory 68K < docs 4.7M）、无过期开放项、无相对时间残留
- verify-scoring.cjs 回归确认仍 16/16 通过

### README 更新
- 新增核心 JS 模块表（common.css / participant.js / scoring.js）
- 新增「综合认知报告」节详细说明

## 第五轮：评分模块验证 + 深链接 + 原始数据摸底

### 评分模块端到端验证
- 新建 `tests/verify-scoring.cjs`：模拟 ParticipantManager，喂入 3 名覆盖 10 项测验的样本数据
- 16 项断言全过：提取正确性 / 域排名 / T 分范围[20,80] / 综合分排序 / 空被试边界
- 验证中发现并修复真实 bug：mccb-scoring.js 混用 `window.ParticipantManager`（检查）与裸 `ParticipantManager`（调用）
  → 浏览器靠 window 全局属性侥幸可用但写法不稳健，统一改为 `window.ParticipantManager`
- 确认 `mccb-participant.js` 第 278 行 `window.ParticipantManager = ParticipantManager` 在脚本加载时挂载，浏览器可用

### comprehensive-report.html 增强
- 新增 `?p=ID` 深链接支持：从被试管理面板可直接跳转某人报告（符合项目 `?p=` 定位被试惯例）
- DOMContentLoaded 后读 URL 参数，匹配到被试则自动选中并渲染

### 原始数据摸底（重要结论）
- `data/cpt-results/` 下 `.rep`/`.dat`/`.raw` 各 383 个，均为 **专有二进制 blob**（.raw 随机字节，.dat 不同被试头部字节一致）
- 无法用文本方式解析，需原厂 CPT 转换工具
- 当前流水线只消费可读的 `.trep` 文本报告（241 个，已转 239 份 HTML）
- 已写入 README「原始数据说明」节，避免后续重复踩坑

### README 更新
- 测试脚本表新增 `verify-scoring.cjs`
- 综合报告节补充 `?p=ID` 深链接 + 原始数据说明

## 第六轮：git 基线 + 全量回归 + 常模可插拔接口（2026-09-09）

### 纳入版本控制（重要里程碑）
- 项目此前**从未纳入 git**——连续多会话改进无任何版本保护
- `git init` + `.gitignore`（忽略可再生的 cpt-reports HTML 与 autotest 报表）+ `.gitattributes`（统一 LF）
- 基线提交 ca38d6d：1466 文件 / 34,716 行（含 data/cpt-results 源数据，不可再生故入库）
- 后续提交 a88dc63：常模接口 + 报告标注 + 测试扩展

### 全量自动化回归
- `node tests/full-battery.cjs --timestamp` → **10/10 全部通过（329s）**
- 印证：近期所有改动（评分模块 / 被试管理 / index 重构 / 颜色体系）均未破坏自动化流程

### 常模（Norm）可插拔接口
- `mccb-scoring.js` 重构：内部相对排名逻辑抽离为默认 `internal` 常模 provider
- 新增 `registerNorm({name,label,apply})` / `setNorm(name)` / `getNorm()` 公共 API
- 未来拿到 MCCB 官方常模可一键切换，无需改业务逻辑
- `getAllProfiles()` 返回 `norm` 字段标注当前常模

### comprehensive-report.html
- 顶部新增 `#normBadge` 常模标注（🛡 项目内部相对排名…），临床透明度
- 用 `data.norm`（来自 getAllProfiles）而非直接 getNorm 填充，与现有数据流一致

### 验证
- `verify-scoring.cjs` 断言 16 → 23，新增常模注册/切换/还原/错误处理用例 → **23/23 通过**
- 踩坑：测试原断言「切回 internal 后 P001 T≠50」误判——P001 恰是中位被试（T=50 合法），改为断言「各被试 T 分有差异」

### 被试管理面板：报告直达按钮
- 每位被试行新增「报告」按钮，深链接 `comprehensive-report.html?p=<ID>`
- 复用上一轮 `?p=` 深链接能力，从管理面板一键查看某人综合认知报告
- 提交 0d69b12
